# Upstream workflow — not used in this fork (no dev branch, no beta repo).
# Retained for reference only; disabled by never-matching branch filter.
name: Mirror dev to beta repo (disabled)

on:
  push:
    branches: ["_disabled_dev"]

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled"
